import styled from 'styled-components'
import '@fontsource/roboto-mono'
export const TextPreviewStyle = styled.section`
  padding: 0 0.5rem;
  white-space: pre-wrap;

  div {
    padding: 1rem 0;
  }

  aside {
    font-family: 'Roboto-mono', monospace;
    padding: 0 0.5rem 0 2rem;
  }
`
